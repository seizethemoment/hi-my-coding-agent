# Step 05: CLI

本章代码对应 `docs/06-step-05-cli.md`。

目标：

- 把 agent loop 接到可交互终端
- 展示用户消息、工具调用、工具结果和 assistant 回复
- 支持基础 slash commands

运行：

```bash
npm install
npm run verify
npm run dev
```

`verify` 使用 mock 模型。真实 CLI 需要配置 `.env`。
