# Step 01: Bootstrap

本章代码对应 `docs/02-step-01-bootstrap.md`。

目标：

- 搭好 TypeScript CLI 工程
- 集中读取环境变量
- 固定启动入口

运行：

```bash
npm install
npm run verify
npm run dev
```

`npm run dev` 需要先复制 `.env.example` 为 `.env` 并填写 `CODING_AGENT_API_KEY`。
